{
  "name": "one-dark-mini-ui",
  "theme": "ui",
  "version": "2.0.0",
  "description": "Minimized Atom One dark UI theme, for better multi pane programming.",
  "keywords": [
    "dark",
    "adaptive",
    "ui"
  ],
  "license": "MIT",
  "repository": "https://github.com/5argon/dark-one-mini-ui",
  "main": "lib/main",
  "engines": {
    "atom": ">0.40.0"
  },
  "devDependencies": {
    "coffeelint": "^1.9.7"
  },
  "configSchema": {
    "fontSize": {
      "title": "Font Size",
      "description": "Change the UI font size. Needs to be between 8 and 20.",
      "type": [
        "integer",
        "string"
      ],
      "minimum": 8,
      "maximum": 20,
      "default": "Auto"
    },
    "tabSizing": {
      "title": "Tab Sizing",
      "description": "In minimum width mode the tabs will be as small as possible but in even width mode, all tabs will be the same size. In auto mode the tabs switch based on the window size.",
      "type": "string",
      "default": "Auto",
      "enum": [
        "Even",
        "Auto",
        "Minimum"
      ]
    }
  }
}
