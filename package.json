{
  "name": "one-dark-ethereal-ui",
  "theme": "ui",
  "version": "1.0.0",
  "description": "Coustomization of Atom One dark UI theme (one-dark-mini-ui)",
  "keywords": [
    "dark",
    "adaptive",
    "ui"
  ],
  "license": "MIT",
  "repository": "https://github.com/psyriccio/dark-one-ethereal-ui.git",
  "main": "lib/main",
  "engines": {
    "atom": ">0.40.0"
  },
  "devDependencies": {
    "coffeelint": "^1.9.7"
  },
  "configSchema": {
    "fontSize": {
      "title": "Font Size",
      "description": "Change the UI font size. Needs to be between 8 and 20.",
      "type": [
        "integer",
        "string"
      ],
      "minimum": 8,
      "maximum": 20,
      "default": "Auto"
    },
    "tabSizing": {
      "title": "Tab Sizing",
      "description": "In minimum width mode the tabs will be as small as possible but in even width mode, all tabs will be the same size. In auto mode the tabs switch based on the window size.",
      "type": "string",
      "default": "Auto",
      "enum": [
        "Even",
        "Auto",
        "Minimum"
      ]
    }
  }
}
